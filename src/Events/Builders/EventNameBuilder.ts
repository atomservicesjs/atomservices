import { ComponentNameBuilder } from "../../Common/Builders/ComponentNameBuilder";
import { Compose } from "../../Common/Compose";

export const EventNameBuilder = (name: string, compose?: Compose) => ComponentNameBuilder(name, compose);
