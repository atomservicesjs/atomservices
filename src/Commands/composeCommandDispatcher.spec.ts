// import { expect } from "chai";
// import * as sinon from "sinon";
// import { composeCommandDispatcher } from "./composeCommandDispatcher";

describe("Commands/composeCommandDispatcher.ts tests", () => {
  describe("#composeCommandDispatcher()", () => {
    it("expect to compose the command dispatcher", () => {
      // arranges

      // acts

      // asserts
    });
  });
});
